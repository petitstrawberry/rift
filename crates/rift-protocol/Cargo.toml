[package]
name = "rift-protocol"
version = "0.1.0"
edition = "2024"
description = "Shared wire protocol types for Rift"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0.201", features = ["derive"] }
serde_json = "1.0.145"
